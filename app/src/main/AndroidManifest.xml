<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.example.mad_qtend">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/Attendance"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MAD_QTEND">
        <activity android:name=".Search" />
        <activity android:name=".Mark_Attendance" />
        <activity android:name=".Delete_Attendance" />
        <activity android:name=".Update_Attendance" />
        <activity android:name=".RVActivity" />
        <activity
            android:name=".attendance"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>